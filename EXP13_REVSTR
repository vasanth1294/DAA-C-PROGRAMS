#include<stdio.h>
#include<string.h>
int main()
{
	int i,j=0;
	char b[10],a[10];
	printf("enter the string:");
	scanf("%s",&a);
	for(i=strlen(a)-1;i>=0;i--){
		b[j]=a[i];
		j++;
	}
	printf("reverse of string is %s",b);
}
